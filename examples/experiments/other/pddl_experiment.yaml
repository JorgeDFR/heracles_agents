metadata: {}
configurations:

    agentic-cypher-pddl:
        dsg_interface:
            dsg_interface_type: none

        pipeline: agentic
        phases:
            main:
                client:
                    client_type: openai
                    # auth key is supplied by HERACLES_OPENAI_API_KEY
                    timeout: 20

                model_info:
                    model: gpt-5-nano
                    temperature: 1.0
                    seed: 123

                agent_info:
                    prompt_settings:
                        base_prompt: $HERACLES_AGENTS_PATH/examples/prompts/cypher/pddl_agent_system_cypher_prompt.yaml
                        output_type: PDDL
                    tools:
                        - name: run_cypher_query
                          bound_args:
                              dsgdb_conf:
                                  dsg_interface_type: heracles
                                  uri: "neo4j://127.0.0.1:7687"

                    tool_interface: openai
                    max_iterations: 6

        questions: $HERACLES_AGENTS_PATH/examples/questions/pddl_questions.yaml